### Guidelines
- When adding css, use Tailwind CSS classes if possible
- A lot of styling is already defined in the Manaring project (../../fireball/frontend). Try to lean on the default styling defined for Manaring instead of overriding it.
